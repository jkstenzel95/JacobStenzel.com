// Fonts
@import url('http://fonts.googleapis.com/css?family=Josefin+Sans:400,600,700')

// Font fix
body {
  -webkit-animation-duration: 0.1s;
  -webkit-animation-name: fontfix;
  -webkit-animation-iteration-count: 1;
  -webkit-animation-timing-function: linear;
  -webkit-animation-delay: 0.1s;
}

@-webkit-keyframes fontfix {
  from { opacity: 1; }
  to { opacity: 1; }
}

// Test things
.menu
  list-style none
  border-bottom 0.1em solid black
  margin-bottom 2em
  padding 0 0 0.5em
  
  
  &:before
    content "["

  &:after 
    content "]"


  & > li 
    display inline


    &:before 
      content "|"
      padding-right 0.3em


    &:nth-child(1):before
      content ""
      padding 0
      
// Real stuff begins here

.logo
  font-family 'Josefin Sans', sans-serif
  font-weight 600
  
  ul
    font-size 48px
    color #608de6
    list-style none
    margin-left 0px
    padding 0px
    
    a
      color #83a6ec
      text-decoration none
      &:hover
        color #A4BEF1
        text-decoration underline
      &:active
        text-decoration underline

    & > li 
      display inline


      &:before 
        content " | "

      &:nth-child(1)
        a
          color #608de6
          text-decoration none
          &:hover
            color #608de6
            text-decoration underline
          &:active
            text-decoration underline

        &:before
          content ""
  
h2.logo
  font-size 40px
  color #afa7d4

vendor-blur(blurAmt)
  -webkit-filter blur(blurAmt)
  -moz-filter blur(blurAmt)
  -o-filter blur(blurAmt)
  -ms-filter blur(blurAmt)
  filter blur(blurAmt)
  
vendor-offset-blur(blurAmt, elemSize)
  vendor-blur(blurAmt)
  position absolute
  left -moz-calc(0 - blurAmt);
  left s('-webkit-calc(0px - %s)', blurAmt);
  left -o-calc(0 - blurAmt);
  left calc(0 - blurAmt);
  top -moz-calc(0 - blurAmt);
  top s('-webkit-calc(0px - %s)', blurAmt);  top -o-calc(0 - blurAmt);
  top -o-calc(0 - blurAmt);
  top calc(0 - blurAmt);
  height -moz-calc(elemSize + 2 * blurAmt);
  height s('-webkit-calc(%s + 2 * %s)', elemSize, blurAmt);
  height -o-calc(elemSize + 2 * blurAmt);
  height calc(elemSize + 2 * blurAmt);
  width -moz-calc(elemSize + 2 * blurAmt);
  width s('-webkit-calc(%s + 2 * %s)', elemSize, blurAmt);
  width -o-calc(elemSize + 2 * blurAmt);
  width calc(elemSize + 2 * blurAmt);
  

html, body
  height 100%
  width 100%

.site-background
  background-image url('images/cube1-960x543.JPG')
  background-size cover
  height -moz-calc(100% + 10px);
  height -webkit-calc(100% + 10px);
  height -o-calc(100% + 10px);
  height calc(100% + 10px);
  width -moz-calc(100% + 20px);
  width -webkit-calc(100% + 20px);
  width -o-calc(100% + 20px);
  width calc(100% + 20px);
  margin-top -5px
  margin-right -10px
  margin-bottom -5px
  margin-left -10px
  vendor-blur(5px)
  
  
.site-background-frame
  overflow hidden
  z-index 1
  height 100%
  width 100%

.site-foreground
  height 100%
  width 100%
  position absolute
  left 0
  top 0
  z-index 2